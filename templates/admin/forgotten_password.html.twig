{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Mot de passe oublié</h1>

    <form action="{{ path('forgotten_password') }}" method="POST">
        <div class="form-group">
            <input placeholder="Adresse email" required name="_username" type="text">
        </div>
        <div class="form-group">
            <button type="submit" name="submit" class="btn btn-success">Renvoyer le mdp</button>
        </div>
    </form>

{% endblock %}